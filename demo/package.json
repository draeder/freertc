{
  "name": "demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:webrtc": "playwright test tests/webrtc.real.e2e.js",
    "test:webrtc:headed": "playwright test tests/webrtc.real.e2e.js --headed",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "js-sha256": "^0.11.1",
    "unsea": "^1.1.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^2.1.9",
    "wrangler": "^3.0.0"
  }
}
